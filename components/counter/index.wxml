<l-counter 
  l-class="container"
  l-symbol-class="all-background font-size"
  l-count-class="all-background inpunt-size"
  l-disabled-class="all-background"
  count="{{count}}" 
  min="{{min}}" 
  max="{{max}}" >
</l-counter>
