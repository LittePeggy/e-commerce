<view class="container">
    <image class="title-img" src="{{banner.img}}" ></image>
    <view class="inner">
        <image class="left" src="{{left.img}}"></image>
        <view class="right-inner" >
            <image class="right-img" src="{{rightTop.img}}"></image>
            <image class="right-img" src="{{rightBottom.img}}"></image>
        </view>
    </view>
</view>
